<script src="https://cdn.tailwindcss.com" defer></script>

{%- style -%}
  /* --- 1. ZÁKLADNÍ NASTAVENÍ SEKCE --- */
  #shopify-section-{{ section.id }} {
      background-color: {{ section.settings.bg_color }};
      color: white;
      position: relative;
      z-index: 10;
      /* Důležité: overflow musí být visible, aby záře mohla jít mimo sekci, pokud je na kraji, 
         ale pro jistotu necháme hidden jen pro celou sekci, aby nerozbila šířku stránky. */
      overflow: hidden; 
      border-top: 1px solid rgba(255,255,255,0.05);
  }

  .footer-wrapper-{{ section.id }} {
      display: flex;
      flex-direction: column;
      gap: 4rem;
      max-width: 1300px;
      margin: 0 auto;
      padding: 4rem 1.5rem;
      position: relative;
      z-index: 10;
      align-items: center;
  }

  @media (min-width: 990px) {
      .footer-wrapper-{{ section.id }} {
          flex-direction: row;
          justify-content: space-between;
          gap: 10rem;
          padding: 6rem 2rem;
      }
      /* Perspective musí být zde pro 3D efekt */
      .footer-left-{{ section.id }} { 
          width: 45%; 
          display: flex; 
          justify-content: center; 
          perspective: 1500px; 
          z-index: 20; 
      }
      .footer-right-{{ section.id }} { width: 50%; }
  }

  /* --- 2. 3D KARTA --- */
  .card-root-{{ section.id }} {
      position: relative;
      width: 320px;
      height: 450px;
      transform-style: preserve-3d;
      transform: rotateX(0deg) rotateY(0deg);
      transition: transform 0.1s ease-out;
      will-change: transform;
      cursor: pointer;
  }

  /* Reset animace */
  .card-root-{{ section.id }}.is-resetting {
      transition: transform 0.5s ease-out;
      transform: rotateX(0deg) rotateY(0deg) !important;
      display: block !important;
  }

  /* --- 3. ZADNÍ ZÁŘE (GLOW BEHIND) --- */
  .card-back-glow-{{ section.id }} {
      position: absolute;
      top: -40px; left: -40px; right: -40px; bottom: -40px; /* Větší než karta */
      border-radius: 50%;
      background: radial-gradient(
          circle at var(--mouse-x, 50%) var(--mouse-y, 50%), 
          rgba(34, 211, 238, 0.6), 
          transparent 50%
      );
      opacity: 0;
      transform: translateZ(-50px); /* Posunuto DOZADU za kartu */
      filter: blur(30px); /* Rozmazání pro efekt světla */
      transition: opacity 0.3s ease;
      pointer-events: none;
      z-index: -1;
      display: block !important;
  }

  .card-root-{{ section.id }}:hover .card-back-glow-{{ section.id }} {
      opacity: 1;
  }

  /* --- 4. HLAVNÍ PLOCHA KARTY (OBRÁZEK) --- */
  .card-face-{{ section.id }} {
      position: absolute;
      inset: 0;
      background-color: #1a1a1a;
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      border-radius: 20px;
      /* Stín karty samotné */
      box-shadow: 0 0 0 1px rgba(255,255,255,0.1), 0 20px 40px rgba(0,0,0,0.8);
      overflow: hidden;
      transform: translateZ(0px); /* Základní rovina */
      display: flex;
      flex-direction: column;
      justify-content: flex-end; /* Text dole */
  }

  /* Přechod přes obrázek, aby byl text čitelný */
  .card-overlay-gradient {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top, rgba(0,0,0,0.95) 10%, rgba(0,0,0,0.5) 40%, transparent 100%);
      z-index: 1;
      display: block !important;
  }

  /* Odlesk na povrchu (Surface Shine) */
  .card-surface-shine-{{ section.id }} {
      position: absolute;
      inset: 0;
      background: radial-gradient(
          800px circle at var(--mouse-x, 50%) var(--mouse-y, 50%), 
          rgba(255, 255, 255, 0.15), 
          transparent 40%
      );
      z-index: 2;
      opacity: 0;
      transition: opacity 0.3s;
      mix-blend-mode: overlay;
      display: block !important;
  }
  .card-root-{{ section.id }}:hover .card-surface-shine-{{ section.id }} {
      opacity: 1;
  }

  /* --- 5. OBSAH KARTY (TEXT) --- */
  .card-content-{{ section.id }} {
      position: relative;
      z-index: 10;
      padding: 2rem;
      transform: translateZ(20px); /* Text vystupuje z karty */
      text-align: center;
  }

  /* --- 6. OSTATNÍ STYLY --- */
  .text-content-{{ section.id }} {
      text-align: left;
      padding-left: 1rem;
      border-left: 1px solid rgba(255,255,255,0.1);
  }
  .font-display { font-family: 'Orbitron', sans-serif; }
  .font-tech { font-family: 'Rajdhani', sans-serif; }
  
  .hover-glow-text { transition: all 0.3s ease; }
  .hover-glow-text:hover {
      text-shadow: 0 0 25px rgba(34,211,238, 0.8), 0 0 40px rgba(217, 70, 239, 0.6);
      transform: scale(1.02);
  }
{%- endstyle -%}

<footer class="w-full relative flex flex-col" id="warp-footer-{{ section.id }}">

    {% if section.settings.bg_image != blank %}
    <div class="absolute inset-0 z-0">
        {%- assign bg_pos_style = 'object-position: ' | append: section.settings.bg_position -%}
        {{ section.settings.bg_image | image_url: width: 2000 | image_tag: class: 'w-full h-full object-cover opacity-40', style: bg_pos_style, loading: 'lazy' }}
        <div class="absolute inset-0 bg-[#020005]/80"></div>
    </div>
    {% endif %}
    <div class="absolute inset-0 bg-gradient-to-t from-cyan-900/10 via-transparent to-transparent opacity-50 pointer-events-none z-1"></div>

    <div class="footer-wrapper-{{ section.id }}">
        
        <div class="footer-left-{{ section.id }}" id="scene-{{ section.id }}">
            
            <div class="card-root-{{ section.id }}" id="card-{{ section.id }}">
                
                <div class="card-back-glow-{{ section.id }}"></div>

                <div class="card-face-{{ section.id }}" 
                     style="background-image: url('{% if section.settings.card_image != blank %}{{ section.settings.card_image | image_url: width: 600 }}{% else %}https://cdn.shopify.com/s/files/1/0533/2089/files/placeholder-images-image_large.png{% endif %}');">
                    
                    <div class="card-overlay-gradient"></div>
                    
                    <div class="card-surface-shine-{{ section.id }}"></div>

                    <div class="card-content-{{ section.id }}">
                        <h3 class="text-3xl font-display font-bold text-white mb-1 drop-shadow-lg tracking-wide">{{ section.settings.card_title }}</h3>
                        <p class="text-cyan-400 font-mono text-xs tracking-[0.2em] uppercase font-bold mb-3">{{ section.settings.card_subtitle }}</p>
                        
                        {% if section.settings.card_desc != blank %}
                        <div class="w-12 h-[2px] bg-cyan-500 mx-auto mb-3"></div>
                        <p class="text-gray-300 text-sm font-tech leading-tight opacity-90">
                            {{ section.settings.card_desc }}
                        </p>
                        {% endif %}
                    </div>
                </div>

            </div>
        </div>

        <div class="footer-right-{{ section.id }}">
            <div class="text-content-{{ section.id }} relative z-20">
                <h2 class="text-5xl md:text-7xl font-display font-black text-transparent bg-clip-text bg-gradient-to-r from-white via-cyan-200 to-white tracking-wider drop-shadow-[0_0_15px_rgba(34,211,238,0.5)] mb-2 hover-glow-text cursor-default">
                    {{ section.settings.main_title }}
                </h2>
                <p class="text-cyan-400 font-mono text-sm uppercase tracking-[0.3em] mb-8">
                    {{ section.settings.subtitle }}
                </p>
                <div class="text-gray-400 text-lg leading-relaxed max-w-lg font-tech">
                    {{ section.settings.description }}
                </div>
            </div>
        </div>
    </div>

    <div class="relative z-30 bg-black/60 backdrop-blur-md border-t border-[#222] py-12 text-lg text-gray-300 font-display leading-relaxed tracking-wide">
        <div class="max-w-[1400px] mx-auto px-6 flex flex-col md:flex-row justify-between items-center gap-6 md:gap-8">
            <div class="whitespace-nowrap opacity-80">
                Copyright &copy; {{ 'now' | date: "%Y" }} {{ section.settings.copyright_text }}. All rights reserved.
            </div>
            <div class="flex items-center flex-wrap justify-center gap-6 md:gap-8 font-bold">
                {% if section.settings.text_1_content != blank %}
                    <a class="hover:text-cyan-400 transition-colors" href="{{ section.settings.text_1_url }}">{{ section.settings.text_1_content }}</a>
                {% endif %}
                {% if section.settings.text_2_content != blank %}
                    <a class="hover:text-cyan-400 transition-colors" href="{{ section.settings.text_2_url }}">{{ section.settings.text_2_content }}</a>
                {% endif %}
                {% if section.settings.terms_url != blank %}
                    <a class="hover:text-cyan-400 transition-colors" href="{{ section.settings.terms_url }}">{{ section.settings.terms_text }}</a>
                {% endif %}
                {% if section.settings.privacy_url != blank %}
                    <a class="hover:text-cyan-400 transition-colors" href="{{ section.settings.privacy_url }}">{{ section.settings.privacy_text }}</a>
                {% endif %}
                <div class="flex items-center gap-4 ml-2 border-l border-white/20 pl-6">
                    {% if section.settings.instagram_url != blank %}
                    <a href="{{ section.settings.instagram_url }}" target="_blank" class="hover:text-cyan-400 transition-colors flex items-center" title="Instagram">
                        <svg class="w-7 h-7" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.069-4.85.069-3.204 0-3.584-.012-4.849-.069-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"></path></svg>
                    </a>
                    {% endif %}
                    {% if section.settings.custom_icon_image != blank %}
                    <a href="{{ section.settings.custom_icon_url }}" class="hover:opacity-80 transition-opacity flex items-center">
                        {{ section.settings.custom_icon_image | image_url: width: 60 | image_tag: class: 'w-7 h-7 object-contain filter grayscale hover:grayscale-0 transition-all' }}
                    </a>
                    {% endif %}
                </div>
            </div>
            <div class="flex items-center gap-2 whitespace-nowrap opacity-80">
                <span class="material-symbols-outlined text-orange-500" style="font-size: 24px;">favorite</span>
                <span>Made with passion and {{ section.settings.made_by_text }}</span>
            </div>
        </div>
    </div>
</footer>

<script>
document.addEventListener("DOMContentLoaded", function() {
    const card = document.getElementById("card-{{ section.id }}");
    const container = document.getElementById("scene-{{ section.id }}");

    if (!card || !container) return;

    container.addEventListener("mousemove", (e) => {
        const rect = card.getBoundingClientRect();
        // Získání pozice myši relativně k samotné kartě
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        // Vypocet procent pro pozici svetla
        // Pokud je myš "mimo" kartu (ale stále v kontejneru), světlo se posune k okraji
        card.style.setProperty("--mouse-x", `${x}px`);
        card.style.setProperty("--mouse-y", `${y}px`);

        // Výpočet rotace (tilt)
        const centerX = rect.width / 2;
        const centerY = rect.height / 2;
        
        // Zvýšíme dělitele (např. 15), aby byl náklon jemnější
        const rotateX = ((y - centerY) / 15) * -1; 
        const rotateY = (x - centerX) / 15;

        card.classList.remove("is-resetting");
        card.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.02, 1.02, 1.02)`;
    });

    container.addEventListener("mouseleave", () => {
        card.classList.add("is-resetting");
        card.style.transform = "rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)";
    });
});
</script>

{% schema %}
{
  "name": "Moje-paticka",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Barva pozadí",
      "default": "#020005"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Obrázek na pozadí sekce"
    },
    {
      "type": "select",
      "id": "bg_position",
      "label": "Zarovnání pozadí",
      "options": [
        { "value": "center center", "label": "Střed (Center)" },
        { "value": "left center", "label": "Vlevo (Left)" },
        { "value": "right center", "label": "Vpravo (Right)" },
        { "value": "center top", "label": "Nahoru (Top)" },
        { "value": "center bottom", "label": "Dolů (Bottom)" }
      ],
      "default": "center center"
    },
    {
      "type": "header",
      "content": "3D Karta (Vlevo)"
    },
    {
      "type": "image_picker",
      "id": "card_image",
      "label": "Hlavní obrázek karty",
      "info": "Tento obrázek bude přes celou kartu."
    },
    {
      "type": "text",
      "id": "card_title",
      "label": "Jméno na kartě",
      "default": "Jan Novák"
    },
    {
      "type": "text",
      "id": "card_subtitle",
      "label": "Role / Titul",
      "default": "Lead Developer"
    },
    {
        "type": "textarea",
        "id": "card_desc",
        "label": "Krátký popis na kartě",
        "default": "Full-stack dev se zaměřením na React a Shopify."
    },
    {
      "type": "header",
      "content": "Hlavní obsah (Vpravo)"
    },
    {
      "type": "text",
      "id": "main_title",
      "label": "Hlavní nápis",
      "default": "COSMIC WEB"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Podnadpis (Malý)",
      "default": "End of Line // Transmission Complete"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Popis pod nadpisem",
      "default": "Jsme digitální architekti budoucnosti."
    },
    {
      "type": "header",
      "content": "Odkazy v patičce"
    },
    {
      "type": "url",
      "id": "instagram_url",
      "label": "Instagram URL"
    },
    {
      "type": "image_picker",
      "id": "custom_icon_image",
      "label": "Vlastní Ikonka"
    },
    {
      "type": "url",
      "id": "custom_icon_url",
      "label": "Odkaz vlastní ikonky"
    },
    {
      "type": "text",
      "id": "text_1_content",
      "label": "Link 1 Text",
      "default": "Ceník"
    },
    {
      "type": "url",
      "id": "text_1_url",
      "label": "Link 1 URL"
    },
    {
      "type": "text",
      "id": "text_2_content",
      "label": "Link 2 Text",
      "default": "Kontakt"
    },
    {
      "type": "url",
      "id": "text_2_url",
      "label": "Link 2 URL"
    },
    {
      "type": "text",
      "id": "terms_text",
      "label": "Terms Text",
      "default": "Terms of Service"
    },
    {
      "type": "url",
      "id": "terms_url",
      "label": "Terms URL"
    },
    {
      "type": "text",
      "id": "privacy_text",
      "label": "Privacy Text",
      "default": "Privacy Policy"
    },
    {
      "type": "url",
      "id": "privacy_url",
      "label": "Privacy URL"
    },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "Copyright Text",
      "default": "Huly Labs"
    },
    {
      "type": "text",
      "id": "made_by_text",
      "label": "Made by Text",
      "default": "Huly"
    }
  ],
  "presets": [
    {
      "name": "Moje-paticka"
    }
  ]
}
{% endschema %}